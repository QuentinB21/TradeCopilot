http:
  routers:
    tradecopilot-auth:
      rule: "PathPrefix(`/auth/admin`) || PathPrefix(`/auth/realms`) || PathPrefix(`/auth/resources`)"
      entryPoints:
        - web
      service: tradecopilot-auth
      priority: 200

    tradecopilot-api:
      rule: "PathPrefix(`/api`) || PathPrefix(`/swagger`)"
      entryPoints:
        - web
      service: tradecopilot-api
      priority: 100

    tradecopilot-client:
      rule: "PathPrefix(`/`)"
      entryPoints:
        - web
      service: tradecopilot-client
      priority: 1

  services:
    tradecopilot-api:
      loadBalancer:
        servers:
          - url: "http://api:8080"

    tradecopilot-auth:
      loadBalancer:
        servers:
          - url: "http://keycloak:8080"

    tradecopilot-client:
      loadBalancer:
        servers:
          - url: "http://client:80"
